#include <stdio.h>
int main()
{
    int n, m;
    scanf("%d %d", &n, &m);
    int a[n];
    for (int i = 0; i < n; i++)
    {
        scanf("%d", a[i]);
    }
    int count[m + 1];
    for (int i = 0; i <= m + 1; i++)
    {
        count[i] = 0;
    }

    for (int i = 0; i <n; i++)
    {
       
    }
    

    return 0;
}